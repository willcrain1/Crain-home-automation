# Copy to .env (git-ignored) in the same folder as docker-compose.yml.
# Long-lived HA access token for the Alexa Matter bridge:
# HA → your profile (bottom-left) → Security → Long-lived access tokens → Create.
MATTER_HUB_TOKEN=paste-token-here
