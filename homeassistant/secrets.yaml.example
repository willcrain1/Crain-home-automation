# Copy to secrets.yaml (git-ignored) and fill in as integrations need them.
# Reference from YAML as: password: !secret reolink_password
reolink_password: "change-me"
# From the InfluxDB first-run setup (http://<NAS_IP>:8086 → API Tokens)
influxdb_token: "change-me"
